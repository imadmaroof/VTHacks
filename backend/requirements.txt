fastapi
uvicorn[standard]
python-multipart

# Optional: enables explanation_layer.py's Gemini-backed explanations +
# doctor talking points (see its use_llm flag). Safe to omit -- without it,
# or without GEMINI_API_KEY set, explanations fall back to the rule-based
# template with no error.
google-genai

# Everything score_upload.py / model_adapter.py / explanation_layer.py need
# (same ML stack the repo already depends on).
-r ../requirements.txt
